version: '3.8'

services:
  aquarium-tycoon:
    build: .
    container_name: aquarium-tycoon
    ports:
      - "8080:80"
    restart: unless-stopped
    labels:
      - "description=Aquarium Tycoon Game Server"
